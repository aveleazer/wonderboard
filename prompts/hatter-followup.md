The sages have asked these questions to the user:

{{SAGE_QUESTIONS}}

Deduplicate, merge related questions, add answer options. Aim for around 5 questions — fewer if questions overlap heavily, more if each is truly distinct.
Return ONLY a JSON array: [{"question": "...", "options": ["...", "..."]}]
Generate in {{LANG}}.
